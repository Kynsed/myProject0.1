namespace myProject
{
    // Port fiel dos enums de input/rumble usados pelo Input/Player.
    public enum RumbleStrength { Light, Medium, Strong, Climb }
    public enum RumbleLength { Short, Medium, Long, FullSecond, TwoSeconds }
    public enum RumbleAmount { Off, Half, On }
    public enum GrabModes { Hold, Invert, Toggle }
    public enum CrouchDashModes { Press, Hold }
}
